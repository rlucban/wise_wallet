import { View } from "react-native";

// Placeholder screen for the central "+" tab button.
// This screen never renders — the tab slot is used purely for the custom FAB button.
export default function AddPlaceholder() {
  return <View />;
}
